interface:
  display_name: "Lumina"
  short_description: "Repo-specific patterns and workflows for lumina"
  default_prompt: "Use the lumina repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true